{
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "dev": "npm run build:index && vite",
    "build": "npm run build:index && tsc -b && vite build",
    "preview": "vite preview",
    "build:index": "npm run prepare:assets && node scripts/build-index.mjs && node scripts/build-search-index.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "^1.21.0",
    "pdfjs-dist": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-icons": "^5.6.0",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
